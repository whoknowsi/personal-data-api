const certificates = require('./certificate')
const deployments = require('./deployment')
const educations = require('./education')
const projects = require('./project')
const workExperiences = require('./workExperience')

module.exports = {
  certificates,
  deployments,
  educations,
  projects,
  workExperiences
}
